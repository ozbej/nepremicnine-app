<%= form_for @place, :html => {:multipart => true} do |f| %>
  <% if place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(place.errors.count, "error") %> prohibited this place from being saved:</h2>

      <ul>
      <% place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ime %><br>
    <%= f.text_field :ime, id: :place_ime %><br><br>
  </div>

  <div class="field">
    <%= f.label :opis %><br>
    <%= f.text_area :opis, id: :place_opis %><br><br>
  </div>

  <div class="field">
    <%= f.label :lokacija %><br>
    <%= f.text_field :lokacija, id: :place_lokacija %><br><br>
  </div>

  <div class="field">
    <%= f.label :kraj %><br>
    <%= f.text_field :kraj, id: :place_kraj %><br><br>
  </div>

  <div class="field">
    <%= f.file_field :image %><br><br> 
  </div>

  <div class="actions">
    <button type="submit" class="btn btn-primary">Dodaj</button><br><br>
  </div>
<% end %>
